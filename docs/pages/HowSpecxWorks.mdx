import { Meta, Subtitle, Title } from "@storybook/blocks"
import { NextPrev } from "@storybookComponents/NextPrev"

<Meta title="Overview/How specx works" />

<Title>How specx works</Title>
<Subtitle>Executable checks and typed project surfaces carry the contract; one skill is optional.</Subtitle>

## Optional navigation skill

The single `specx` skill helps an agent inspect a project, use its commands, and follow structured
diagnostics. It is not required to use the package or understand a generated project.

Install it with `npx skills add`. Its canonical source is the repository's `skills/specx/` directory.

## Project initializer

`specx init` renders one small framework-neutral Python project. The starter proves the package
layout, strict tooling, a core health action, DIWire composition, and mirrored unit tests. It does not
select FastAPI, SQLAlchemy, a database, or a deployment model.

The initializer is intentionally narrow. Later changes are ordinary source changes, or typed
use-case scaffolds exposed by `specx project`, verified by the same checks.

## Foundation library

The `specx.core.foundation`, `specx.delivery.foundation`, and
`specx.infrastructure.foundation` modules provide explicit base categories. A project class declares
what kind of work it owns by inheriting the relevant base: use case, pure service, gateway,
controller, settings, and so on.

Import bases from their defining modules; foundation package roots do not re-export them:

```python
from specx.core.foundation.dto import BaseDTO
from specx.delivery.foundation.controller import BaseController
```

## Architecture checker

`specx check` scans Python files under `src/` and `tests/`, applies the selected static rules, and
reports violations with semantic IDs, remediation hints, and documentation links. The same engine is available through
`specx.testing.architecture` for Python tests and project-specific rules.

The checker is a guardrail, not a Python interpreter or type checker. It recognizes imports, class
ancestry, calls, names, paths, and specific documentation patterns. Ruff, mypy, tests, migration
checks, security review, and human design judgment still cover different failure modes.

## How the surfaces meet

```text
developer request
      │
      ▼
agent or developer inspects project commands and public guides
      │
      ▼
project code inherits specx foundation categories
      │
      ▼
Ruff + typing + tests + specx check validate the change
```

Executable checks are the correctness contract. An existing service can start with the checker, and
teams can use the bases, project CLI, generated guidance, and documentation without any skill.

<NextPrev current="overview-how-specx-works" />
