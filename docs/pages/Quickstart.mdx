import { Meta, Subtitle, Title } from "@storybook/blocks"
import { NextPrev } from "@storybookComponents/NextPrev"

<Meta title="Overview/Quickstart" />

<Title>Quickstart</Title>
<Subtitle>Create a framework-neutral service, inspect it, and run the same checks an agent will use.</Subtitle>

## Prerequisites

Install [uv](https://docs.astral.sh/uv/getting-started/installation/). The package declares Python
3.11 or newer, and the initializer defaults to Python 3.14. Node.js is only needed to install the
optional navigation skill.

## Optional: install the navigation skill

The following command installs the single optional skill for Codex:

```sh
npx skills add maksimzayats/specx --skill specx --agent codex -y
```

Skill installation and Python package installation are separate. The package provides the
initializer, foundation classes, project commands, and enforceable checks.

## Initialize a project

```sh
uvx specx init order-service
cd order-service
make check
```

The initializer creates a new directory, adds `specx` and `diwire`, adds the development tools, and
lets uv write the lockfile. It refuses to overwrite a non-empty directory. Use `--no-sync` when you
only want the files and will manage dependencies yourself.

The result includes a small health slice:

```text
src/order_service/
  core/health/
    dtos/health_status_dto.py
    enums/health_status_enum.py
    services/health_status_service.py
    use_cases/check_health.py
  ioc/container.py
tests/unit/core/health/
AGENTS.md
Makefile
pyproject.toml
```

There is no FastAPI app, database, migration directory, or empty placeholder layer. Add those only
when the service needs them.

## Run the guardrails directly

```sh
uv run specx check --output-format json
uv run specx rule list
uv run specx rule explain use-cases.return-dtos
```

`specx check` discovers the single importable package below `src/`. A clean run exits with status 0,
architecture violations exit with status 1, and configuration or execution errors exit with status
2.

## Make the first change

Ask the agent for the outcome and require the project checks:

```text
Add a read-only quote action under core/orders. Return an OrderQuoteDTO, add focused unit tests,
run `specx check --output-format json`, follow its hints, and finish with `make check`.
```

You may mention `$specx` when you want the optional navigation workflow, but correctness never
depends on skill installation.

Next, [build your first core feature](/docs/tutorial/build-your-first-core-feature/). For
command and scaffold details, see [CLI](/docs/reference/cli/) and
[Generated project](/docs/reference/generated-project/).

<NextPrev current="overview-quickstart" />
