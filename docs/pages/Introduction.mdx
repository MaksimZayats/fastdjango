import { Meta, Subtitle, Title, Unstyled } from "@storybook/blocks"
import { DocsCard } from "@storybookComponents/DocsCard"
import { HideTableOfContents } from "@storybookComponents/HideTableOfContents"
import { CliIcon, FoundationIcon, GuardrailsIcon, SkillsIcon } from "@storybookComponents/Icons"

<HideTableOfContents />

<Meta title="Overview/Introduction" />

<Title>specx</Title>
<Subtitle>Executable architecture rules and typed project foundations for Python services.</Subtitle>

specx gives coding agents a concrete way to build and change Python backends without letting
framework, persistence, and application concerns drift together. It combines four surfaces:

- **Structured diagnostics** explain architecture failures and link to authoritative rule guides.
- **The project initializer** renders a strict framework-neutral starting point.
- **Foundation classes** give project classes explicit architectural categories.
- **Architecture checks** inspect the result and report semantic rule IDs.

The checks are the correctness contract. The optional `specx` skill only helps an agent navigate
these surfaces; it does not replace normal design review, tests, or framework documentation.

## Start here

<Unstyled>
  <div className="specx-card-grid">
    <DocsCard
      href="/docs/overview/quickstart/"
      icon={<CliIcon />}
      title="Quickstart"
      subtitle="Initialize a service and run its first checks."
    />
    <DocsCard
      href="/docs/tutorial/build-your-first-core-feature/"
      icon={<FoundationIcon />}
      title="First core feature"
      subtitle="Build one complete framework-neutral feature."
    />
    <DocsCard
      href="/docs/guides/install-agent-skills/"
      icon={<SkillsIcon />}
      title="Agent skills"
      subtitle="Install the optional project-navigation workflow."
    />
    <DocsCard
      href="/docs/reference/architecture-rules/"
      icon={<GuardrailsIcon />}
      title="Guardrails"
      subtitle="See what specx checks and how to configure it."
    />
  </div>
</Unstyled>

## Where specx fits

Use specx when a Python service needs explicit application boundaries that coding agents can follow
and CI can verify. The catalog covers framework-neutral cores, FastAPI delivery, DIWire composition,
SQLAlchemy and Alembic, runtime settings, external adapters, and test structure.

specx is deliberately opinionated. Use cases take one command or query and return DTOs. Transactional
work enters through a unit-of-work manager. Delivery and infrastructure stay outside the inner core.
Project classes inherit a foundation category that states their role.

If those constraints do not match the service you want to build, adopt individual ideas rather than
disabling most of the rule set.

## Typical ways to use it

- Start a new service with `specx init`, then use project commands and structured diagnostics.
- Install the Python package in an existing `src/`-layout service and introduce guardrails gradually.
- Use the foundation classes and checks without installing any agent skill.
- Install the optional skill when an agent benefits from a concise project-navigation workflow.
